The program counts how many times each query string appears in the main list of strings. It loops through every 
query, checks how many times it matches strings in the list, and stores those counts in a result list, which is 
then printed line by line.
